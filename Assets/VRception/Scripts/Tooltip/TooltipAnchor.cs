namespace VRception
{
    /// <summary>
    /// Enum that stores all different controller anchor points
    /// </summary>
    public enum TooltipAnchor
    {
        DEFAULT,    // Anchor point is the center of hte controller itself
        INDEX_TRIGGER,
        HAND_TRIGGER,
        THUMBSTICK,
        BUTTON_ONE,
        BUTTON_TWO
    }
}
